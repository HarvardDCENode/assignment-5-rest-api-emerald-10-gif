h2.text-center.mb-4 Upload a Photo

.container.d-flex.justify-content-center
  .col-md-6
    .card.p-4.shadow-sm
      if flashMsg.length > 0
        .alert.alert-danger <strong>FLASH!</strong>#{flashMsg}
      form(method='POST', action="/photos", enctype="multipart/form-data")
        .form-group
          label(for='name') Photo Title:
          input#name.form-control(type='text', placeholder='photo title', name='title')
        .form-group
          label(for='description') Description:
          input#description.form-control(type='text', placeholder='description', name='description')
        .form-group
          label(for='image') Image:
          input#image.form-control(type='file', name='image')
        button.btn.btn-primary.mt-3(type='submit') Post Your Photo